function solucao(min, max, valores) {
      let resposta = [];
      
      for (let i = 0; i < valores.length; i++){
           if (valores[i] >= min && valores[i] <= max){
              resposta.push(valores[i]);         
    }
        } console.log(resposta)
      
    }
  